import { ReadableEnumPipe } from './readable-enum.pipe';

describe('ReadableEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
